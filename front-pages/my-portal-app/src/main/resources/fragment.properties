pltp.fragment.service=http://fragment.test-pltp.com/pltp-fragment-service

# connect timeout value, in milliseconds
pltp.fragment.connect.timeout=1000
# read timeout value, in milliseconds
pltp.fragment.read.timeout=3000