package com.ctfo.upp.gateway.fastpay.utils;

import static com.ctfo.upp.gateway.fastpay.utils.encrypt.RSA.sign;

public final class SignUtil {

//	static public String buildSignOfYB(final String beanOfValues) {
//		// 从配置文件读取商户自己的私钥
//		if (beanOfValues == null || beanOfValues.isEmpty())
//			throw new IllegalArgumentException("Invalid parameter.");
//		//final String merchantPrivateKey = BeanUtil.getFastPaymentConfig().getZJ_MOBILE_FAST_PAY_PRIVATE_KEY();
//		//final String merchantPrivateKey = ConfigUtil.get;
//		if (merchantPrivateKey == null || merchantPrivateKey.isEmpty())
//			throw new IllegalStateException("私钥无效.");
//		return sign(beanOfValues, merchantPrivateKey);
//	}
}
